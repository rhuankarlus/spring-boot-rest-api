CREATE SEQUENCE id_seq_audit AS BIGINT START WITH 1 INCREMENT BY 1;
CREATE TABLE audit (
  id        INTEGER      GENERATED BY DEFAULT AS SEQUENCE id_seq_audit PRIMARY KEY,
  url       VARCHAR(255),
  content   VARCHAR(255) NOT NULL,
  type      INTEGER      NOT NULL,
  date_time TIMESTAMP
);