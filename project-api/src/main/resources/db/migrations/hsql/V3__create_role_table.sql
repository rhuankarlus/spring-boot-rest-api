CREATE SEQUENCE id_seq_role AS BIGINT START WITH 1 INCREMENT BY 1;
CREATE TABLE role (
  id        INTEGER      GENERATED BY DEFAULT AS SEQUENCE id_seq_audit PRIMARY KEY,
  name      VARCHAR(255) UNIQUE NOT NULL
);